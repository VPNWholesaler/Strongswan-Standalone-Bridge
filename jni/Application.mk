# select the ABI(s) to build for (see CPU-ARCH-ABIS.html in the NDK docs).
#APP_ABI := x86 mips x86_64
APP_ABI := armeabi-v7a arm64-v8a x86_64 mips x86
APP_PLATFORM := android-16
NDK_TOOLCHAIN_VERSION := 4.9
